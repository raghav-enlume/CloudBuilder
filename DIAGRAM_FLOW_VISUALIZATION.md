# CloudBuilder - Data Flow & Diagram Architecture

## 🎯 Complete System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          CLOUDBUILDER ECOSYSTEM                             │
└─────────────────────────────────────────────────────────────────────────────┘

                              ┌──────────────────┐
                              │  USER INTERFACE  │
                              └────────┬─────────┘
                                       │
                    ┌──────────────────┼──────────────────┐
                    │                  │                  │
              ┌─────▼─────┐    ┌──────▼──────┐    ┌─────▼─────┐
              │  Upload    │    │  Properties │    │  Canvas   │
              │  Toolbar   │    │   Panel     │    │  Viewer   │
              └─────┬─────┘    └─────────────┘    └───────────┘
                    │
                    │ (JSON File)
                    │
        ┌───────────▼───────────┐
        │   FILE VALIDATION     │
        │  (Toolbar.tsx)        │
        └───────────┬───────────┘
                    │
        ┌───────────▼───────────┐
        │  FORMAT DETECTION     │
        ├───────────┬───────────┤
        │ AWS Format│ DB Flat   │
        │ (Region)  │ Array     │
        └────┬──────┴──┬────────┘
             │         │
             │    ┌────▼──────────────────┐
             │    │ dbJsonParser.ts       │
             │    │ convertDBJsonToAWSFmt │
             │    └────┬─────────────────┘
             │         │
             └────┬────┘
                  │
        ┌─────────▼──────────────┐
        │  AWS Region Format     │
        │ {region: {resources}}  │
        └─────────┬──────────────┘
                  │
        ┌─────────▼──────────────┐
        │ awsDataParser.ts       │
        │ parseAWSDataJSON()     │
        └─────────┬──────────────┘
                  │
      ┌───────────┼───────────┐
      │           │           │
  ┌───▼───┐  ┌───▼───┐  ┌───▼────┐
  │ NODES │  │ EDGES │  │ LAYOUT │
  └───┬───┘  └───┬───┘  └───┬────┘
      │          │          │
      │          │     ┌────▼──────────────┐
      │          │     │ layoutEngine.ts   │
      │          │     │ Hierarchical      │
      │          │     │ Layout Calc       │
      │          │     └────┬──────────────┘
      │          │          │
      └──────────┼──────────┘
                 │
        ┌────────▼──────────┐
        │ React Flow        │
        │ - Nodes           │
        │ - Edges           │
        │ - Positions       │
        │ - Styling         │
        └────────┬──────────┘
                 │
        ┌────────▼──────────┐
        │ INTERACTIVE       │
        │ DIAGRAM           │
        │ ✓ Pan & Zoom      │
        │ ✓ Click Nodes     │
        │ ✓ Edit Props      │
        │ ✓ View Edges      │
        └───────────────────┘
```

---

## 🔄 DB Flat Array to AWS Format Conversion

```
┌──────────────────────────────────────────────────────────────┐
│                    DB FLAT ARRAY FORMAT                      │
│                   (clean-db-14.json)                         │
└──────────────────────────────────────────────────────────────┘
        │
        │ [
        │   {
        │     "region": "sample-web-app",
        │     "total_resources": 17,
        │     "resources": [ <-- FLAT ARRAY
        │       { resource_type: "VPC", resource_property: {...} },
        │       { resource_type: "SUBNET", resource_property: {...} },
        │       { resource_type: "EC2", resource_property: {...} },
        │       { resource_type: "RDS", resource_property: {...} },
        │       { resource_type: "S3", resource_property: {...} },
        │       ...
        │     ]
        │   }
        │ ]
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│        dbJsonParser.convertDBJsonToAWSFormat()               │
│        ──────────────────────────────────────                │
│  Convert flat array to hierarchical structure                │
│  Map resource_type → AWS array                               │
│  Extract resource_property fields                            │
└──────────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│                 AWS REGION FORMAT OUTPUT                     │
│               (Hierarchical Structure)                       │
└──────────────────────────────────────────────────────────────┘
        │
        │ {
        │   "sample-web-app": {
        │     vpcs: [
        │       { VpcId: "vpc-webapp-001", CidrBlock: "10.0.0.0/16" }
        │     ],
        │     subnets: [
        │       { SubnetId: "subnet-public-001", VpcId: "vpc-webapp-001" },
        │       { SubnetId: "subnet-private-001", VpcId: "vpc-webapp-001" }
        │     ],
        │     instances: [
        │       { InstanceId: "i-webapp-001", VpcId: "vpc-webapp-001" }
        │     ],
        │     rds_instances: [
        │       { DBInstanceIdentifier: "rds-webapp-postgres", VpcId: "..." },
        │       { DBInstanceIdentifier: "rds-webapp-mysql", VpcId: "..." }
        │     ],
        │     s3_buckets: [
        │       { Name: "webapp-assets-bucket" },
        │       { Name: "webapp-backups-bucket" }
        │     ],
        │     ...
        │   }
        │ }
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│        awsDataParser.parseAWSDataJSON()                      │
│        Ready for node/edge generation                        │
└──────────────────────────────────────────────────────────────┘
```

---

## 📊 Node Creation Flow

```
┌─────────────────────────────────────────────────────────────┐
│  INPUT: AWS Region Format                                   │
│  {region: { vpcs, subnets, instances, rds, s3, ... }}      │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 1: CREATE REGION CONTAINER NODE                       │
│  ├─ ID: region-{regionKey}                                  │
│  ├─ Width: Full                                             │
│  ├─ Height: Dynamic (based on content)                      │
│  └─ Color: Light blue background                            │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 2: CREATE VPC CONTAINER NODES (inside region)         │
│  ├─ ID: vpc-{VpcId}                                         │
│  ├─ Width: 1100px (max)                                     │
│  ├─ Height: Dynamic (based on subnets)                      │
│  ├─ Padding: 140px                                          │
│  └─ Color: Slightly darker blue                             │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 3: CREATE SUBNET CONTAINER NODES (inside VPC)         │
│  ├─ ID: subnet-{SubnetId}                                   │
│  ├─ Width: 380px                                            │
│  ├─ Type: Public (at top) or Private (below)                │
│  ├─ Height: 120px + content                                 │
│  └─ Color: Green (public) or Red (private)                  │
│                                                              │
│  Public Subnets:     Positioned at Y = VPC_Y + padding      │
│  Private Subnets:    Positioned below public subnets        │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 4: CREATE EC2 INSTANCE NODES (inside subnets)         │
│  ├─ ID: instance-{InstanceId}                               │
│  ├─ Width: 140px                                            │
│  ├─ Height: 80px                                            │
│  ├─ Position: Vertically stacked, left side                 │
│  └─ Color: Orange                                           │
│                                                              │
│  Layout inside subnet:                                      │
│  [Instance1]                                                │
│  [Instance2]                                                │
│  [Instance3]                                                │
│  (Margin between instances: 10px)                           │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 5: CREATE NAT/IGW NODES (inside subnets)             │
│  ├─ ID: nat-{NatGatewayId}                                  │
│  ├─ ID: igw-{InternetGatewayId}                             │
│  ├─ Position: Bottom-right inside subnet                    │
│  ├─ Width: 160px                                            │
│  ├─ Height: 80px                                            │
│  └─ Color: Blue (NAT), Purple (IGW)                         │
│                                                              │
│  Layout inside subnet:                                      │
│  ┌─────────────────────────┐                               │
│  │   [EC2s on left]        │                               │
│  │                         │                               │
│  │              [NAT/IGW]  │                               │
│  │              (bottom)   │                               │
│  └─────────────────────────┘                               │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 6: CREATE RDS NODES (inside subnets)                  │
│  ├─ ID: rds-{db_instance_name}                              │
│  ├─ Position: Bottom-right inside subnet                    │
│  ├─ Width: 160px                                            │
│  ├─ Height: 80px                                            │
│  └─ Color: Teal                                             │
│                                                              │
│  (Positioned next to NAT/IGW if space available)            │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 7: CREATE SECURITY GROUP NODES (floating in VPC)      │
│  ├─ ID: sg-{GroupId}                                        │
│  ├─ Position: Right side of VPC (outside subnets)           │
│  ├─ Width: 120px                                            │
│  ├─ Height: 60px                                            │
│  └─ Color: Yellow                                           │
│                                                              │
│  Layout in VPC:                                             │
│  ┌──────────────────────────────────────┐                  │
│  │  [Subnets...]              [SGs...]  │                  │
│  │                                      │                  │
│  │  [VPC Container]                    │                  │
│  └──────────────────────────────────────┘                  │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 8: CREATE ROUTE TABLE NODES (floating in VPC)         │
│  ├─ ID: rt-{RouteTableId}                                   │
│  ├─ Position: Below security groups                         │
│  ├─ Width: 120px                                            │
│  ├─ Height: 60px                                            │
│  └─ Color: Light gray                                       │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 9: CREATE REGION-LEVEL RESOURCE NODES                 │
│  ├─ S3 Buckets (ID: s3-{BucketName})                        │
│  ├─ Lambda Functions (ID: lambda-{FunctionName})            │
│  ├─ DynamoDB Tables (ID: ddb-{TableName})                   │
│  ├─ API Gateways (ID: api-{ApiId})                          │
│  ├─ Load Balancers (ID: lb-{LBName})                        │
│  ├─ CloudFront Dists (ID: cf-{DistId})                      │
│  └─ Grid layout: 8 resources per row                        │
│     (positioned below VPCs)                                 │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│  RESULT: Complete Node Array                                │
│  [                                                           │
│    { id: "region-sample-web-app", ... },                    │
│    { id: "vpc-vpc-webapp-001", ... },                       │
│    { id: "subnet-public-001", ... },                        │
│    { id: "instance-i-webapp-001", ... },                    │
│    { id: "rds-rds-webapp-postgres", ... },                  │
│    { id: "s3-webapp-assets-bucket", ... },                  │
│    ...                                                       │
│  ]                                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔗 Edge Creation Flow

```
┌──────────────────────────────────────────────────────────┐
│  INPUT: All Nodes Created                                │
│  Ready to create connections                             │
└──────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────┐
│  PHASE 1: EC2 → RDS CONNECTIONS (Red #FF6B6B)            │
│  ┌────────────────────────────────────────────────────┐  │
│  │ For each EC2 instance:                             │  │
│  │   For each RDS instance:                           │  │
│  │     if instance.VpcId == rds.VpcId:               │  │
│  │       create edge(EC2, RDS, RED)                  │  │
│  │       label: "Connects to"                         │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  Result: EC2 ──[RED]──> RDS (same VPC)                  │
└──────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────┐
│  PHASE 2: EC2 → NAT GATEWAY (Purple #8C4FFF)             │
│  ┌────────────────────────────────────────────────────┐  │
│  │ For each EC2 instance:                             │  │
│  │   if instance in PUBLIC subnet:                    │  │
│  │     for each NAT Gateway in VPC:                   │  │
│  │       create edge(EC2, NAT, PURPLE)               │  │
│  │       label: "Routes via NAT"                      │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  Result: EC2 ──[PURPLE]──> NAT (outbound traffic)       │
└──────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────┐
│  PHASE 3: EC2 → IGW (Green #1DB954)                      │
│  ┌────────────────────────────────────────────────────┐  │
│  │ For each EC2 instance:                             │  │
│  │   if instance in PUBLIC subnet:                    │  │
│  │     for each IGW in VPC:                           │  │
│  │       create edge(EC2, IGW, GREEN)                │  │
│  │       label: "Internet Access"                     │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  Result: EC2 ──[GREEN]──> IGW (internet access)         │
└──────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────┐
│  PHASE 4: EC2 → S3 BUCKETS (Orange #FFA500)              │
│  ┌────────────────────────────────────────────────────┐  │
│  │ For each EC2 instance:                             │  │
│  │   for each S3 bucket in region:                    │  │
│  │     create edge(EC2, S3, ORANGE)                  │  │
│  │     label: "Access"                                │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  Result: EC2 ──[ORANGE]──> S3 (all buckets)             │
└──────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────┐
│  PHASE 5: ROUTE TABLE CONNECTIONS (Gray #4ECDC4)         │
│  ┌────────────────────────────────────────────────────┐  │
│  │ For each Route Table:                              │  │
│  │   For each Route:                                  │  │
│  │     if target is IGW:                              │  │
│  │       create edge(RT, IGW, GRAY)                  │  │
│  │     if target is NAT:                              │  │
│  │       create edge(RT, NAT, GRAY)                  │  │
│  │     if target is TGW/PCX:                          │  │
│  │       create edge(RT, TGW/PCX, GRAY)              │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  Result: RT ──[GRAY]──> IGW/NAT/TGW (routing)           │
└──────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────┐
│  PHASE 6: APPLICATION CONNECTIONS                        │
│  ├─ API Gateway → Lambda (Orange #FF9900)               │
│  ├─ Lambda → DynamoDB (Blue #146EB4)                    │
│  ├─ ECS → RDS (Blue #146EB4)                            │
│  ├─ Load Balancer → ECS (Blue #146EB4)                  │
│  └─ Other service connections                           │
└──────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────┐
│  RESULT: Complete Edge Array                             │
│  [                                                        │
│    { source: "instance-i-webapp-001",                    │
│      target: "rds-rds-webapp-postgres",                  │
│      stroke: "#FF6B6B" },  // EC2 → RDS                 │
│    { source: "instance-i-webapp-001",                    │
│      target: "nat-nat-webapp-001",                       │
│      stroke: "#8C4FFF" },  // EC2 → NAT                 │
│    { source: "instance-i-webapp-001",                    │
│      target: "igw-igw-webapp-001",                       │
│      stroke: "#1DB954" },  // EC2 → IGW                 │
│    { source: "instance-i-webapp-001",                    │
│      target: "s3-webapp-assets-bucket",                  │
│      stroke: "#FFA500" },  // EC2 → S3                  │
│    ...                                                    │
│  ]                                                        │
└──────────────────────────────────────────────────────────┘
```

---

## 📦 clean-db-14.json Visual Architecture

```
┌─────────────────────────────────────────────────────────────┐
│           SAMPLE WEB APP INFRASTRUCTURE                     │
│              (14 AWS Resources)                             │
└─────────────────────────────────────────────────────────────┘

┌────────────────── AWS Region: sample-web-app ──────────────┐
│                                                              │
│  ┌──────────────────── VPC: vpc-webapp-001 ──────────────┐ │
│  │ CIDR: 10.0.0.0/16                                     │ │
│  │                                                        │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │ PUBLIC SUBNET: subnet-public-001                 │ │ │
│  │  │ CIDR: 10.0.1.0/24                                │ │ │
│  │  │ AZ: us-east-1a                                   │ │ │
│  │  │                                                  │ │ │
│  │  │  ┌────────────────┐                             │ │ │
│  │  │  │  EC2 Instance  │                             │ │ │
│  │  │  │ i-webapp-001   │                             │ │ │
│  │  │  │ t3.medium      │────┐                        │ │ │
│  │  │  │ 10.0.1.100     │    │                        │ │ │
│  │  │  └────────────────┘    │                        │ │ │
│  │  │         ▲               │                        │ │ │
│  │  │         │               ▼                        │ │ │
│  │  │    [RED to RDS]  ┌──────────────┐               │ │ │
│  │  │    [PURPLE to NAT] │  NAT Gate   │              │ │ │
│  │  │    [GREEN to IGW]  │nat-webapp   │              │ │ │
│  │  │    [ORANGE to S3]  └──────────────┘              │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  │                                                        │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │ PRIVATE SUBNET: subnet-private-001              │ │ │
│  │  │ CIDR: 10.0.2.0/24                               │ │ │
│  │  │ AZ: us-east-1b                                  │ │ │
│  │  │                                                 │ │ │
│  │  │  ┌─────────────────────────────────────────┐   │ │ │
│  │  │  │  PostgreSQL RDS                         │   │ │ │
│  │  │  │  rds-webapp-postgres                    │   │ │ │
│  │  │  │  db.t3.micro                            │   │ │ │
│  │  │  │  Port: 5432                             │   │ │ │
│  │  │  │  ◀─── EC2 (RED LINE)                    │   │ │ │
│  │  │  └─────────────────────────────────────────┘   │ │ │
│  │  │                                                 │ │ │
│  │  │  ┌─────────────────────────────────────────┐   │ │ │
│  │  │  │  MySQL RDS                              │   │ │ │
│  │  │  │  rds-webapp-mysql                       │   │ │ │
│  │  │  │  db.t3.micro                            │   │ │ │
│  │  │  │  Port: 3306                             │   │ │ │
│  │  │  │  ◀─── EC2 (RED LINE)                    │   │ │ │
│  │  │  └─────────────────────────────────────────┘   │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  │                                                        │ │
│  │  Floating Resources (inside VPC):                    │ │
│  │  ┌────────────────┐ ┌────────────────┐              │ │
│  │  │  Security Grp  │ │  Security Grp  │              │ │
│  │  │  sg-web-001    │ │  sg-rds-001    │              │ │
│  │  └────────────────┘ └────────────────┘              │ │
│  │  ┌────────────────┐ ┌────────────────┐              │ │
│  │  │  Route Table   │ │  Route Table   │              │ │
│  │  │  rtb-public    │ │  rtb-private   │              │ │
│  │  └────────────────┘ └────────────────┘              │ │
│  │  ┌────────────────┐ ┌────────────────┐              │ │
│  │  │  IGW           │ │                │              │ │
│  │  │  igw-webapp    │ │                │              │ │
│  │  └────────────────┘ └────────────────┘              │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                              │
│  Region-Level Resources:                                    │
│  ┌──────────────────┐  ┌──────────────────┐               │
│  │    S3 Bucket     │  │    S3 Bucket     │               │
│  │ webapp-assets    │  │ webapp-backups   │               │
│  │ ◀──── EC2        │  │ ◀──── EC2        │               │
│  │ (ORANGE LINES)   │  │ (ORANGE LINES)   │               │
│  └──────────────────┘  └──────────────────┘               │
│                                                              │
└─────────────────────────────────────────────────────────────┘

KEY CONNECTIONS:
├─ EC2 (public subnet)
│  ├─── RDS (red line)           - Database access
│  ├─── NAT (purple line)        - Outbound traffic
│  ├─── IGW (green line)         - Internet access
│  └─── S3 (orange line x2)      - Storage access
├─ Route Tables
│  ├─── Public RT → IGW          - Public routing
│  └─── Private RT → NAT         - Private routing
└─ Security Groups
   ├─ sg-web-001 (EC2 rules)     - Port 80, 443
   └─ sg-rds-001 (RDS rules)     - Port 5432 (PG), 3306 (MySQL)

RESOURCE COUNT: 17 total
├─ Networking: VPC, 2 Subnets, IGW, NAT, 2 Route Tables = 7
├─ Security: 2 Security Groups = 2
├─ Compute: 1 EC2 Instance = 1
├─ Database: 2 RDS Instances = 2
├─ Storage: 2 S3 Buckets = 2
├─ Load Balancing: 1 Target Group, 1 ALB Listener = 2
└─ Other: DB Subnet Group, RDS Backup Job = 2
```

---

## 🎨 Color Legend

```
┌──────────────────────────────────────────────────────────┐
│              CONNECTION COLORS & MEANINGS                 │
├──────────────────────────────────────────────────────────┤
│                                                            │
│  🔴 RED (#FF6B6B)           ──  EC2 → RDS                 │
│     Label: "Connects to"                                  │
│     Meaning: Database access (same VPC)                   │
│                                                            │
│  🟣 PURPLE (#8C4FFF)         ──  EC2 → NAT                │
│     Label: "Routes via NAT"                               │
│     Meaning: Outbound traffic from public subnet          │
│                                                            │
│  🟢 GREEN (#1DB954)          ──  EC2 → IGW               │
│     Label: "Internet Access"                              │
│     Meaning: Internet access from public subnet           │
│                                                            │
│  🟠 ORANGE (#FFA500)         ──  EC2 → S3                │
│     Label: "Access"                                       │
│     Meaning: S3 bucket access (via VPC Endpoint)         │
│                                                            │
│  🔵 BLUE (#146EB4)           ──  Lambda → DynamoDB       │
│     Label: "Accesses"                                     │
│     Meaning: Service integration                          │
│                                                            │
│  ⚪ GRAY (#4ECDC4)           ──  Route Table → IGW/NAT    │
│     Label: "Route to {CIDR}"                              │
│     Meaning: Routing configuration                        │
│                                                            │
│  🟡 YELLOW                   ──  Security Group            │
│     (Container box)                                       │
│     Meaning: Security rules boundary                      │
│                                                            │
│  🟩 GREEN Container          ──  PUBLIC Subnet             │
│     Meaning: Internet-accessible subnet                   │
│                                                            │
│  🟥 RED Container            ──  PRIVATE Subnet            │
│     Meaning: Internet-restricted subnet                   │
│                                                            │
│  🟦 BLUE Container           ──  VPC Container             │
│     Meaning: Virtual Private Cloud boundary               │
│                                                            │
│  🟪 LIGHT BLUE Container     ──  AWS REGION                │
│     Meaning: Geographic region boundary                   │
│                                                            │
└──────────────────────────────────────────────────────────┘
```

---

## 📍 Positioning Algorithm Summary

```
HIERARCHICAL LAYOUT RULES:
==========================

LEVEL 0: REGION CONTAINER
├─ Position: (0, currentY)
├─ Width: 100% of diagram
├─ Height: Dynamic (sum of all resources + margins)
└─ Margin below: 200px + (VPC count * 50px)

LEVEL 1: VPC CONTAINERS (inside Region)
├─ Position: (regionX + padding, vpcY)
├─ Horizontal spacing: 1100px (max width) + 100px (margin)
├─ Number per row: Based on diagram width (typically 1)
├─ Width: 1100px (max)
├─ Height: 120px + (subnet height x subnet count)
└─ Padding inside: 140px

LEVEL 2: SUBNET CONTAINERS (inside VPC)
├─ PUBLIC subnets positioned first (at top)
├─ PRIVATE subnets positioned below public
├─ Position: (vpcX + padding, subnetY)
├─ Horizontal spacing: 380px (width) + 100px (margin)
├─ Width: 380px
├─ Height: 120px + content height
└─ Padding inside: 10px

LEVEL 3: RESOURCE NODES (inside Subnet)
├─ EC2 Instances:
│  ├─ Position: Top-left, stacked vertically
│  ├─ Width: 140px
│  ├─ Height: 80px
│  └─ Vertical spacing: 10px
├─ NAT Gateways:
│  ├─ Position: Bottom-right corner
│  ├─ Width: 160px
│  └─ Height: 80px
├─ Load Balancers:
│  ├─ Position: Bottom-right corner
│  ├─ Width: 160px
│  └─ Height: 100px
└─ RDS Instances:
   ├─ Position: Bottom-right corner
   ├─ Width: 160px
   └─ Height: 80px

FLOATING NODES (inside VPC, outside Subnets)
├─ Security Groups:
│  ├─ Position: Right side of VPC
│  ├─ Width: 120px
│  ├─ Height: 60px
│  └─ Vertical spacing: 80px
└─ Route Tables:
   ├─ Position: Below security groups
   ├─ Width: 120px
   └─ Height: 60px

REGION-LEVEL RESOURCES (below VPCs)
├─ S3 Buckets
├─ Lambda Functions
├─ DynamoDB Tables
├─ API Gateways
├─ Load Balancers
└─ Layout: Grid (8 per row)
   └─ Position: (startX, regionResourcesY)
   └─ Width: 120px each
   └─ Height: 80-100px
   └─ Horizontal margin: 20px
   └─ Vertical margin: 20px
```

---

## 🚀 Full Request → Diagram Generation Timeline

```
Timeline: User uploads clean-db-14.json
══════════════════════════════════════

T+0ms: User clicks "Upload" button
  ├─ File selection dialog opens
  └─ User selects clean-db-14.json

T+100ms: File read starts
  ├─ FileReader API reads file as text
  └─ Content loaded into memory

T+150ms: JSON parsing
  ├─ File content → JavaScript object
  └─ Validation: Is it valid JSON?

T+200ms: Format detection
  ├─ Check: Is it array format?
  ├─ Answer: YES (clean-db-14.json is array)
  └─ Route to dbJsonParser

T+250ms: DB Flat Array conversion
  ├─ convertDBJsonToAWSFormat() called
  ├─ Process 17 resources
  │  ├─ VPC: vpc-webapp-001
  │  ├─ Subnets: 2 resources
  │  ├─ EC2: 1 resource
  │  ├─ RDS: 2 resources
  │  ├─ S3: 2 resources
  │  ├─ IGW: 1 resource
  │  ├─ NAT: 1 resource
  │  ├─ Route Tables: 2 resources
  │  ├─ Security Groups: 2 resources
  │  ├─ Target Group: 1 resource
  │  ├─ ALB Listener: 1 resource
  │  └─ Other: 2 resources
  └─ Convert to AWS region format

T+300ms: Converted data ready
  ├─ Output: { "sample-web-app": { vpcs, subnets, ... } }
  └─ Pass to awsDataParser

T+350ms: Node creation starts
  ├─ Create Region node
  ├─ Create VPC nodes (1)
  ├─ Create Subnet nodes (2)
  ├─ Create EC2 nodes (1)
  ├─ Create NAT nodes (1)
  ├─ Create IGW nodes (1)
  ├─ Create RDS nodes (2)
  ├─ Create Security Group nodes (2)
  ├─ Create Route Table nodes (2)
  ├─ Create S3 nodes (2)
  ├─ Create Target Group nodes (1)
  ├─ Create ALB Listener nodes (1)
  ├─ Create other nodes (2)
  └─ Total: 18 nodes created

T+450ms: Layout calculation
  ├─ Calculate Region container size
  ├─ Calculate VPC container positions
  ├─ Calculate Subnet positions
  ├─ Calculate Resource positions (EC2, RDS, etc.)
  ├─ Calculate floating node positions (SGs, RTs)
  └─ All position data finalized

T+500ms: Edge creation
  ├─ EC2 → RDS connections (red)
  ├─ EC2 → NAT connections (purple)
  ├─ EC2 → IGW connections (green)
  ├─ EC2 → S3 connections (orange x2)
  ├─ Route Table connections (gray)
  └─ Total: 8 edges created

T+550ms: Data structure ready
  ├─ nodes: Array of 18 node objects
  ├─ edges: Array of 8 edge objects
  └─ Pass to React Flow

T+600ms: React Flow rendering
  ├─ Mount React Flow component
  ├─ Initialize canvas
  ├─ Render nodes with custom styling
  ├─ Render edges with custom styling
  ├─ Apply zoom/pan handlers
  └─ Diagram visible on screen!

T+700ms: User interaction ready
  ├─ Pan diagram
  ├─ Zoom in/out
  ├─ Click nodes to view properties
  ├─ Click edges to see connections
  └─ Edit resource properties

Total latency: ~600-700ms (from upload to visible diagram)
```

This completes the comprehensive diagram understanding for your entire CloudBuilder project!

